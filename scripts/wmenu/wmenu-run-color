#!/usr/bin/sh
set -eu

. "$HOME/.config/wmenu/color.sh"

pgrep -x wmenu > /dev/null && exit 1 ||
timeout 10 wmenu-run -i \
  -F \
  -E "SourceCodePro regular 13" \
  -C "LXGW WenKai Mono 13" \
  -l 10 \
  -N "$colorbg" \
  -S "$colorsbg" \
  -s "$colorbg" \
  -M "$colorsbg" \
  -m "$colorbg" \
  -p "Run " \
  "$@"
